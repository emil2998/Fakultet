<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.foi.air.air2116">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_romic_promet_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_romic_promet_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RomicPrometApp">
        <activity
            android:name=".StorageActivity"
            android:exported="false" />
        <activity
            android:name=".fragment.Profile"
            android:exported="false" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>